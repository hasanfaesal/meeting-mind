- Package manager: both bun.lock and package-lock.json exist, but per user rules, default to pnpm unless repo rules differ. However, the presence of both lockfiles is notable — but the repo doesn't specify which to use. Since the user's global rule says "JavaScript/package scripts: pnpm", but the repo has bun.lock and package-lock.json, I should note this ambiguity. Actually, since bun.lock is present and Next 16 with Turbopack, maybe bun is used. But package-lock.json is also present. I should probably ask or just mention both exist and prefer pnpm per global rules? Wait, the instructions say "Match existing project conventions first." The existence of both lockfiles doesn't tell me which is actively used. I'll note the ambiguity and recommend using the lockfile present, but default to pnpm if installing fresh? Actually, the user said "Default stack and patterns: JavaScript/package scripts: pnpm." But "Match existing project conventions first." Since I can't be sure, I should ask about preferred package manager or note both lockfiles exist.
Wait, but the instructions say: "Only ask the user questions if the repo cannot answer something important." Both bun.lock and package-lock.json exist; this is ambiguous. It's important because running the wrong install command can corrupt lockfiles. I should ask the user which package manager they actually use.